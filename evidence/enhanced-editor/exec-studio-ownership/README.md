# Studio ownership on shared030

Two targeted host tests and pinned native build PASS. Individual coordinated
production-allocator fixtures PASS with rc0 and done markers: mixer1 and sampler11
explicit Fast/not-Chip allocations, zero pool-owned bytes on final release.
Production zero-budget refusal leaves Fast policy unchanged. Each explicit pointer
is checked using TypeOfMem; runtime library allocations are not instrumented.

Exact run/launcher cleanup completed and AmiConnect window released. No UI,
restart, startup or physical operation. These are ownership/correctness checks,
not audio-device transport, deadlines, real-time16-voice or physical performance
acceptance. Native editor scheduling/output remain unwired.
