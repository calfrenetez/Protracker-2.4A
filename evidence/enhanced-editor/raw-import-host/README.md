Host ASan/UBSan RAW streaming import checks pass for16 explicit formats, multiblock exact masters, allocator/fill/limit refusal, interrupted/short reads, undo/redo and zero leaks. Existing sampler regression passes. Full pinned native build passes. RAW import runtime NOT RUN; no physical or UI acceptance. Encoded input uses3072-byte stack block, decoded staging and undo use existing budgeted allocator. WAV/IFF/module import remains whole-input.
